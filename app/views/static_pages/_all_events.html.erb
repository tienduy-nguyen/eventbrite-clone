 <div class="tab-pane fade show active" 
 id="home" role="tabpanel" aria-labelledby="all_events-tab">
 
 <% if @all_events.nil? %>
    <div class="mt-4">
      No events to show here
    </div>
  <% else %>
   <% @all_events.each do |event|%>
        event.title
    <% end %>
 <% end %>
 
 </div>